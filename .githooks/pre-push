#!/usr/bin/env bash
# pre-push — full test suite before anything hits a remote.
# ~5 seconds on this package; catches real regressions instead of
# deferring the failure to CI.

set -e

cd "$(git rev-parse --show-toplevel)"

echo "[pre-push] jest"
npx --no-install jest --silent

echo "[pre-push] OK"
